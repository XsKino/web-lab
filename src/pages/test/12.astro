---
import Body from '@layouts/Body.astro'
import Container from '@components/test/12/Container.astro'
---

<script>
  const $containers = document.querySelectorAll('#container')

  $containers?.forEach($container => {
    $container.addEventListener('click', e => {
      const $actives = document.querySelectorAll('.active')
      $actives.forEach($active => {
        if ($active !== $container) $active.classList.remove('active')
      })

      $container?.classList.toggle('active')
    })
  })
</script>

<Body class='dark bg-black' title='FAQ Collapse'>
  <div
    class='fixed top-0 left-0 -translate-x-1/3 -translate-y-1/3 h-[100vmin] w-[100vmin] blur-[250px] rounded-full bg-purple-400/50'>
  </div>
  <div
    class='fixed bottom-0 right-0 translate-x-1/3 translate-y-1/3 h-[100vmin] w-[100vmin] blur-[250px] rounded-full bg-blue-400/50'>
  </div>
  <div
    class='fixed top-0 left-0 -translate-x-1/3 -translate-y-1/3 h-[70vmin] w-[70vmin] blur-[250px] rounded-full bg-purple-200/50 animate-pulse [animation-duration:10s]'>
  </div>
  <div
    class='fixed bottom-0 right-0 translate-x-1/3 translate-y-1/3 h-[70vmin] w-[70vmin] blur-[250px] rounded-full bg-blue-200/50 animate-pulse [animation-duration:10s]'>
  </div>
  <main class='pt-4 flex flex-col justify-evenly items-center md:pt-8 min-h-screen'>
    <h1 class='font-bold text-2xl md:text-3xl text-center pt-8 md:pt-4'>FAQ Section</h1>
    <div class='flex flex-col p-8 gap-5 w-full max-w-screen-md items-stretch'>
      <Container title='Why did the computer go to therapy?'>
        It had too many bytes of emotional baggage!
      </Container>

      <Container title="What's a programmer's favorite snack?">Chips and Code!</Container>

      <Container title='How many programmers does it take to change a light bulb?'>
        None, that's a hardware issue!
      </Container>

      <Container title='Why do programmers prefer dark mode?'>Light attracts bugs!</Container>

      <Container title='How does a computer catch a cold?'>It gets a bit too close to the Windows!</Container>

      <Container title='Why did the developer break up with Git?'>It couldn't commit!</Container>

      <Container title='How do you comfort a JavaScript bug?'>You console it!</Container>

      <Container title='Why did the coder stay in school forever?'>
        He just couldn't get enough classes!
      </Container>
    </div>
  </main>
</Body>

<style>
  .fixed {
    z-index: -1;
  }
</style>
